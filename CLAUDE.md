# 시설 인력 관리 앱 — Claude Code 프로젝트 지침

> **이 파일을 읽었으면 "CLAUDE.md 확인 완료"라고 먼저 말해줘.**  
> 코드 작성 전 반드시 이 파일 전체를 읽고 숙지할 것.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 앱 목적 | 부동산 관리 회사의 현장 인력(보안/청소/시설유지보수) 업무 관리 시스템 |
| UI 언어 | **한국어 전용** — 버튼, 레이블, 오류 메시지, 안내 문구 모두 한국어 |
| 플랫폼 | 모바일 우선 반응형 웹 앱 |
| 직원 기기 | 스마트폰 (Android/iOS) — 390px 기준 |
| 관리자 기기 | 데스크탑 PC — 1440px 기준 |

---

## 2. 사용자 구분

### 현장 직원 (일반 사용자)
- 직군: 보안 경비 / 청소 직원 / 시설유지보수 기사
- 디지털 숙련도: **낮음** — 단순하고 직관적인 UI 필수, 3탭 이내 핵심 기능 접근
- 주요 행동 흐름: 로그인 → 업무 목록 확인 → 업무 수행 → 사진+메모로 완료 보고 제출

### 관리자 (어드민 사용자)
- 복수 관리자 지원, **모든 관리자 동일 권한** (슈퍼어드민 구분 없음)
- 주요 행동 흐름: 업무 생성/배정 → 실시간 현황 모니터링 → 완료 보고 검토(승인/반려)

---

## 3. 핵심 기능 요약

| 기능 | 설명 |
|---|---|
| 직원 대시보드 | 우선순위 정렬 업무 목록 뷰 + 주간/월간 캘린더 뷰 |
| 완료 보고 | 사진(최대 5장) + 메모(10~500자) 제출 → 관리자 승인/반려 |
| 관리자 포털 | 업무 생성·배정(반복 설정 포함), 실시간 보고 검토, 인력 관리, 아카이브 |
| 실시간 갱신 | **SSE(Server-Sent Events)** 방식 — 브라우저 열어둔 동안 자동 갱신, 푸시 알림 없음 |
| 사진 저장 | 클라우드 스토리지, 보존 기간 제한 없음 (수동 삭제 시까지 영구 보관) |

### 업무 우선순위 및 상태

**우선순위**: 높음 / 보통 / 낮음  
**업무 상태**: 미완료 → 진행 중 → 완료 대기(관리자 검토 중) → 완료 / 재작업 필요

---

## 4. 제외 기능 (구현 금지)

- ❌ GPS 위치 추적
- ❌ 오프라인 모드 / Service Worker 캐싱
- ❌ 브라우저 푸시 알림 (Web Push API)
- ❌ 데이터 내보내기 (엑셀/CSV)
- ❌ 네이티브 앱 (iOS/Android)
- ❌ 영문 UI 텍스트

---

## 5. 디자인 시스템

> **designs/ 폴더의 HTML 파일이 각 화면의 픽셀 기준 레퍼런스다.**  
> 색상, 간격, 컴포넌트 스타일은 반드시 아래 시스템과 이 HTML 파일을 기준으로 구현할 것.

### 5.1 선택된 디자인 방향: "실용 모바일"
청록 포인트 + 흰 배경의 명료하고 경쾌한 스타일. 높은 가독성과 큰 터치 타겟 우선.

### 5.2 색상 팔레트

| 역할 | 값 | 용도 |
|---|---|---|
| 주색 (Primary) | `oklch(55% 0.14 195)` | 버튼, 링크, 활성 탭, 강조 |
| 위험 (Danger) | `oklch(62% 0.16 25)` | 반려, 긴급, 삭제, 높음 우선순위 |
| 배경 | `#FFFFFF` | 페이지 기본 배경 |
| 카드 배경 | `oklch(95% 0.005 220)` | 카드, 입력 필드 배경 |
| 텍스트 (Primary) | `oklch(18% 0.01 260)` | 본문, 제목 |
| 텍스트 (Secondary) | `oklch(50% 0.01 260)` | 보조 설명, 날짜 |
| 테두리 | `oklch(88% 0.008 240)` | 구분선, 카드 테두리 |
| 성공 (Success) | `oklch(62% 0.15 160)` | 완료, 승인, 낮음 우선순위 |
| 경고 (Warning) | `oklch(75% 0.16 85)` | 진행 중, 보통 우선순위 |

**상태별 색상 코딩 (전 화면 통일)**  
- 우선순위: 높음 = Danger / 보통 = Warning / 낮음 = Success  
- 업무 상태: 완료 대기 = Primary / 재작업 필요 = Danger / 완료 = 회색

### 5.3 타이포그래피

- **서체**: Noto Sans KR (Google Fonts) — 반드시 이 폰트 사용
- **굵기**: 400 본문 / 600 강조 / 700 제목
- **최소 글꼴**: 14px (모바일)
- **아이콘**: 반드시 텍스트 레이블 병기 (아이콘 단독 사용 금지)

### 5.4 컴포넌트 규칙

```css
/* 카드 */
border-radius: 12px;
box-shadow: 0 1px 4px oklch(0% 0 0 / 8%);

/* 버튼 / 입력 */
border-radius: 8px;
min-height: 48px; /* 터치 타겟 최소값 — 절대 줄이지 말 것 */

/* 애니메이션 */
transition: 150ms ease;
```

- 상태 표시는 반드시 **색상 + 텍스트 병기** (색맹 접근성 필수)
- '재작업 필요' 카드는 좌측 세로 강조 바 + 연한 빨강 배경

### 5.5 레이아웃

**모바일 (직원 화면)**
- 기준 너비: 390px (iPhone 14 Pro)
- 네비게이션: 하단 탭바
- 카드형 업무 목록 (큰 글씨, 넉넉한 여백)

**데스크탑 (관리자 화면)**
- 기준 해상도: 1440px
- 네비게이션: 좌측 고정 사이드바
- 데이터 테이블 + 카드 혼합 레이아웃

---

## 6. 화면 목록 및 디자인 파일 매핑

> 구현 전 반드시 해당 HTML 파일을 읽고 시작할 것.

### 직원 화면 (모바일)

| 화면 ID | 화면명 | 디자인 파일 |
|---|---|---|
| SCR-E01 | 로그인 | `designs/SCR-E01-login.html` |
| SCR-E02 | 업무 목록 (홈) | `designs/SCR-E02-task-list.html` |
| SCR-E03 | 캘린더 뷰 | `designs/SCR-E03-calendar.html` |
| SCR-E04 | 업무 상세 | `designs/SCR-E04-task-detail.html` |
| SCR-E05 | 완료 보고 입력 | `designs/SCR-E05-report-input.html` |
| SCR-E06 | 제출 확인 | `designs/SCR-E06-report-confirm.html` |

### 관리자 화면 (데스크탑)

| 화면 ID | 화면명 | 디자인 파일 |
|---|---|---|
| SCR-A01 | 관리자 로그인 | `designs/SCR-A01-admin-login.html` |
| SCR-A02 | 메인 대시보드 | `designs/SCR-A02-dashboard.html` |
| SCR-A03 | 업무 목록 관리 | `designs/SCR-A03-task-management.html` |
| SCR-A04 | 업무 생성·편집 | `designs/SCR-A04-task-form.html` |
| SCR-A05 | 보고 검토 목록 | `designs/SCR-A05-report-list.html` |
| SCR-A06 | 보고 상세 검토 | `designs/SCR-A06-report-review.html` |
| SCR-A07 | 인력 관리 | `designs/SCR-A07-staff.html` |
| SCR-A08 | 업무 아카이브 | `designs/SCR-A08-archive.html` |

---

## 7. 개발 규칙

### 7.1 구현 순서 원칙
1. **UI 먼저, API 나중** — 화면 전체를 더미 데이터로 완성 후 API 연결
2. 직원 화면(E01→E06) 완성 후 관리자 화면(A01→A08) 진행
3. 각 화면 구현 전 반드시 해당 designs/ HTML 파일 확인

### 7.2 한국어 처리
- 모든 텍스트 상수는 별도 파일(`/src/constants/messages.ko.ts` 또는 동등한 위치)에 모아서 관리
- 한국어 IME 입력 시 `compositionstart` / `compositionend` 이벤트 처리 필수
- 날짜 형식: `YYYY년 MM월 DD일`, 시간: 24시간제 (`HH:MM`)
- 문자열 정렬: 가나다 순

### 7.3 SSE 구현 지침
- 관리자 대시보드(SCR-A02) 및 보고 검토 목록(SCR-A05)에 SSE 연결
- 연결 끊김 시 자동 재연결 로직 포함
- UI에 연결 상태 표시 (`LIVE` 배지 — 연결 중 / 끊김 상태 구분)

### 7.4 사진 업로드
- 최대 5장, 파일당 최대 10MB
- 허용 형식: JPG, PNG, HEIC
- 업로드 전 클라이언트 사이드 리사이징 적용 (원본 그대로 전송 금지)
- 업로드 진행률 표시 필수

### 7.5 접근성
- 터치 타겟 최소 48px 준수 — 예외 없음
- 색상만으로 상태를 구분하는 UI 금지 (텍스트 병기 필수)
- 입력 필드에 `label` 태그 또는 `aria-label` 필수

---

## 8. 작업 요청 템플릿

> 화면 하나를 구현할 때 아래 형식으로 요청할 것.

```
designs/SCR-[ID]-[name].html 파일을 읽고,
이 디자인을 그대로 구현해줘.

지금은 UI만 (더미 데이터 사용).
API 연동은 [나중에 / 이번에] 함께 진행.
```

---

## 9. 절대 하지 말 것

- 영문 UI 텍스트 사용 (변수명·주석은 영어 가능, UI 표시 텍스트는 한국어만)
- `min-height: 48px` 미만 터치 타겟
- 아이콘 단독 사용 (텍스트 레이블 없이)
- GPS / 오프라인 / 푸시 알림 관련 코드 추가
- designs/ 파일을 읽지 않고 화면 구현 시작
- Noto Sans KR 외 한국어 서체 사용
